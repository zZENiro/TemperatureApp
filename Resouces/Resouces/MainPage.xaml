<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:local="clr-namespace:Resouces"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Resouces.MainPage">

    <ScrollView>
        <StackLayout>
            <Button Command="{Binding UpdateData}"/>
            <Label Text="{Binding WeatherName}"/>
            <Label Text="{Binding Temperature}"/>
            <Label Text="{Binding MaxTemperature}"/>
            <Label Text="{Binding MinTemperature}"/>
            <Label Text="{Binding Pressure}"/>
            <Label Text="{Binding Humidity}"/>
        </StackLayout>
    </ScrollView>

</ContentPage>
